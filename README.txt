PontoApp (Minimal API .NET 8)
- Registre ponto agora (entrada/saída) ou manualmente
- Persiste em data/pontos.json
- UI simples em / (lista, edita, apaga)
- Exporta CSV em /api/export.csv

Como rodar:
dotnet run --urls "http://localhost:5090"
Abra http://localhost:5090/
